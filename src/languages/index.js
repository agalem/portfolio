import en from "./en";
import pl from "./pl";

export const dictionaryList = {
    en,
    pl
};

export const languageOptions = [
    { id: "en", text: "English" },
    { id: "pl", text: "Polski"}
];

